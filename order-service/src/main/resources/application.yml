spring:
  kafka:
    bootstrap-servers: localhost:9192
  cloud:
    stream:
      kafka:
        binder:
          brokers:
          - localhost:9192
      bindings:
        orderCreated-out-0:
          destination: order-topic
          content-type: application/json        
  application:
    name: order-service

server:
  port: 8082
